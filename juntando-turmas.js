const salaDePython = ['Melissa', 'Helena', 'Rodrigo'];
const salaJavaDeJavaScript = ['Juliana', 'Leonardo', 'Raquel'];

const salasUnificadas = salaDePython.concat(salaJavaDeJavaScript)//método concat faz a unificação de duas arrays. Como parametro eu coloco a segundo array a ser unificada

console.log(salasUnificadas);